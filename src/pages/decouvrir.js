import React from 'react';
import { graphql } from 'gatsby';

import Layout from '../components/layout/Layout';

const Decouvrir = ({ data }) => <Layout>Decouvrir</Layout>;

export default Decouvrir;
